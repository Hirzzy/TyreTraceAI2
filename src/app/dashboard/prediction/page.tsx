
"use client";

import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function PredictionPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Page Supprimée</CardTitle>
        <CardDescription>Cette fonctionnalité a été retirée.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Le contenu de cette page a été supprimé comme demandé.</p>
      </CardContent>
    </Card>
  );
}
